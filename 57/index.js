let a=prompt("Enter FIRST string")
let b=prompt("Enter SECOND string")
let result  = a.toUpperCase()===b.toUpperCase()
if(result){
    document.write("Your strings are same")
}
else{
    document.write("Your strings are not same")
}